# 演示报告：`demo_only`

本目录只存放流程/门禁演示工件。

- 一律标注 `scope: demo_only`（`demo_only`）。
- **不得**作为真实研究、模型输入、组合事实或 Decision 证据。
- 生产查询、筛选 run 与 Decision 输入清单必须排除本目录及任何`demo_only`工件。

`unknown` 演示是停止条件/预检证明：证明关键输入缺失时正确阻断（`unknown`）；不代表 Modeling 或 Decision 已通过。
